#!/usr/bin/env node

require('path').exists(__dirname+'/../scripts/'+process.argv[2]+'.js', function(exists) {
  if (exists) {
    require(__dirname+'/../scripts/'+process.argv[2])();
  }
  else {
    console.log("Unable to find a scipt for this command: " + process.argv[2]); 
  }
});